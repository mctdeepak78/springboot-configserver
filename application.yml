eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9000/eureka
  instance:
    hostname: localhost
    
spring:
  jpa:
    hibernate:
        ddl-auto : none
  datasource:
    url : jdbc:mysql://localhost/vollzo
    username : admin
    password : PlmQaz#112233
